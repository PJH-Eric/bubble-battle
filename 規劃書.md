# 泡泡大作戰（bubble-battle）規劃書

> **遊戲正式名稱：泡泡大作戰**，專案代號 **`bubble-battle`**。
> （企劃期間暫稱「爆爆王」，但那是 Nexon《Crazy Arcade》的商標中文名，公開專案不沿用。）

> 狀態：**規劃中，尚未實作**。本文件是實作前的共識文件，確認後再開工。
> 沿用 `C:\Eric\AI Agent` 既有小遊戲的架構慣例（Node + Express + Socket.IO、
> `public/js/rules.js` 共用規則核心、`lib/rooms.js` 房間邏輯、`public/js/config.js`
> 唯一 server URL 入口、`tests/verify.js`、`scripts/*-check.js`、`render.yaml`、`啟動遊戲.bat`）。

---

## 0. 已確認的決定

| 項目 | 決定 |
|---|---|
| 淘汰規則 | **經典水球**：中招先變成「水球泡泡」被困住，可自救／被隊友救；泡泡狀態再被爆才真的淘汰 |
| 移動模型 | **平滑像素移動（真即時）**，伺服器固定 tick 權威模擬，前端預測＋插值 |
| 對戰模式 | 個人混戰 **2–4 人**（核心）、個人混戰擴充 **6–8 人**、**組隊 2v2／3v3** |
| 角色 | 外觀不同、**能力完全相同**（純靠道具拉開差距） |
| 地圖 | **固定手設地圖** 與 **隨機生成地圖** 兩種，由房間設定選擇 |
| AI | **幼幼班／簡單／普通／困難** 四段 |
| 語言 | 介面、註解、文件、commit 全繁體中文 |
| 視覺 | 全手繪向量 SVG，不使用 emoji 文字美術 |
| 地圖尺寸 | 個人 2–4 人 **15×13**；6–8 人與組隊 **17×15** |
| 局時 | **3 分鐘**，最後 **45 秒** 開始水位上升 |
| 互救 | 個人混戰**不能互救**，泡泡縮短為 **3.5 秒**；組隊模式泡泡 5 秒且隊友可救 |
| 負面道具 | **做，而且預設開啟**（幼幼班強制關閉，房主可關） |
| 斷線 | 超過 30 秒 **直接判定離場**，重連後以觀戰者身分回房 |
| 版控 | **現在就建立公開 GitHub repo**，比照 fruit-link |
| 大廳 | 遊戲完成後 **一併加入既有 `game-lobby`** |
| repo 名稱 | **`bubble-battle`**（公開） |
| 角色主題 | **可愛動物園** 8 隻：貓、狗、兔、熊、熊貓、企鵝、青蛙、小雞 |
| 觸控預設 | **左下虛擬搖桿**＋右下放水球大鈕（設定裡可改成全螢幕滑動） |
| 教學 | **靜態圖文說明頁**（SVG 圖解規則與道具圖鑑），不做互動練習關 |
| 幼幼班 | **規則完全一致，只有 AI 變弱**（不放寬泡泡、水位上升或自爆） |
| 地圖主題 | **六套以上，可持續擴充**，做成純資料檔 |
| 局數 | **單局定勝負**，結束回房間由房主開下一局 |
| 音效 | **以泡泡／水聲為主軸** 設計 |
| 房間隱私 | **只有公開房＋邀請連結**，不做密碼房 |
| 觀戰 | 觀戰者**可以聊天**、**可以看到全場資訊**（不做排隊入座） |
| 戰績 | 本機保存**簡單統計**，不上傳、不做排行榜 |
| 踢水球 | **要做，但排到 M4**，等同步穩定後再加 |
| 角色識別 | 自己腳下光環＋頭頂暱稱；組隊再加隊伍顏色 |
| 結算畫面 | **勝利者大頭貼＋一行數據**，不做完整排名表 |
| 暱稱／角色 | 記住上次選的；**同房角色可以重複** |
| 爆炸預告 | **不做範圍預告**，只靠水球越跳越快提示（原版硬派手感） |
| 開局 | **全員按「準備好」**房主才能開始 |
| 色彩輔助 | **用形狀區分，不只靠顏色**，預設就這樣做 |
| 冷啟動 | 大廳顯示**「喚醒伺服器中」泡泡動畫**與進度提示 |
| 出生點 | **四角→四邊的固定對稱位**，不隨機 |
| 淘汰掉落 | 被淘汰時**噴出一半道具**到附近格子供人撿 |
| AI 顯示 | **名字＋難度都顯示**，例如「小花貓（困難）」 |
| 手機直向 | 仍可玩，但**提示轉成橫向比較好玩**（可關閉） |
| 聊天 | **自由輸入＋快捷短語**（不會打字也能溝通） |
| 房主權 | **可踢人**、**房主離開自動換人**（不做強制轉觀戰） |
| 語言 | **只做繁體中文**（照既有小遊戲慣例） |
| 音訊預設 | **背景音樂與音效都預設開**（首次手勢後才會真的播放） |
| 首頁 | **大主視覺圖＋三顆按鈕**：一個人玩／跟別人玩／怎麼玩 |
| 單機人數 | **可選 1～7 個電腦對手**，每個都能單獨設難度 |
| 淘汰後 | **自動轉觀戰看完這局**，可聊天 |
| 連鎖 | **不給額外獎勵**，純粹是戰術與畫面效果 |
| 遊戲名稱 | **泡泡大作戰** |
| 固定地圖 | **6 張純地形**，不加傳送門／輸送帶／水池等機關 |
| 快速加入 | **要做**，找不到房就自動開一間等人 |
| 邀請連結 | **不設時限**，與房間同生命；房主可撤銷重發 |
| 單機規則 | **與線上完全一致**（一樣 3 分鐘、一樣水位上升） |
| 同機雙人 | **不做**，一台裝置一個人 |
| 建房預設 | 人數上限**預設 4 人**（可調到 8） |

---

## 1. 玩法規則

### 1.1 場地

- 格子地圖：**15×13**（個人混戰 2–4 人）／**17×15**（個人 6–8 人與組隊 2v2、3v3）。
- 三種格子：
  - **硬塊**：不可破壞、擋水柱。固定排列在偶數列偶數行（經典棋盤骨架）。
  - **軟箱**：可被水柱炸掉，有機率掉落道具。
  - **空地**：可行走。
- 出生點與其相鄰的 2 格保證淨空（不會一開場就被自己炸到）。
- **出生點是固定對稱位，不隨機**：2 人用對角、4 人用四角，人數再多就往四邊中點長，
  每個位置到最近對手的距離與可用逃生路徑都經過檢查，開場不會有人先天被夾死。
  組隊模式時同隊分配在相鄰的出生點。
- **手設固定地圖 6 張，全部純地形**：靠硬塊與軟箱的排列做出差異（迷宮型、開闊型、雙路型、口字型、十字型、蜂巢型），
  **不加傳送門、輸送帶、水池等機關**。規則單純、平衡好調，AI 也不必處理特殊地形。

### 1.2 水球與爆炸

| 項目 | 規則 |
|---|---|
| 放置 | 站在空地按放球鍵，水球落在**所在格的中心**，玩家可以從自己剛放的水球上走出去，離開後就不能再走回去 |
| 引爆 | 放置後 **3.0 秒** 自動爆炸。**不預告水柱範圍**——只用水球越跳越快、最後一秒抖動加劇來提示，威力範圍要自己記 |
| 水柱 | 上下左右十字，每方向長度＝威力值（初始 1 格） |
| 阻擋 | 硬塊完全擋住；軟箱吸收一格並被破壞；另一顆水球被打到會**立即連鎖引爆** |
| 連鎖 | 連鎖走同一個 tick 內的佇列，避免無限遞迴。**連鎖不給任何額外獎勵**（不加分、不提高掉寶率），純粹是戰術與畫面效果 |
| 水柱殘留 | 爆炸特效存在 **0.5 秒**，期間仍具傷害判定 |

### 1.3 中招 → 泡泡 → 淘汰（核心）

1. 玩家碰到水柱 → 進入 **泡泡狀態**（`trapped`）：原地被水球包住、無法移動、不能放球。
2. 泡泡計時：**個人混戰 3.5 秒**／**組隊模式 5 秒**。
   - **自救**（兩種模式都有）：連續左右交替按方向鍵可加快掙脫。個人混戰每次有效輸入扣 **0.3 秒**、組隊扣 0.25 秒，皆有 0.12 秒輸入冷卻避免連打作弊。
   - **被救**：**只有組隊模式**才有。隊友走到泡泡格上，泡泡立即破裂。
     個人混戰**不開放互救**——沒有人能救你，也沒有人能靠救人翻盤，被困就是拚掙脫或等時間到。
   - **時間到**：自動脫困。
3. 脫困後獲得 **1.5 秒無敵**（閃爍），期間不受水柱傷害、也不能放球。
4. 泡泡狀態中**再被水柱打到** → **淘汰出局**。

> 這個設計就是「被救得回來、被補刀就沒了」的原版節奏，也讓組隊模式的救援有意義。

### 1.4 勝負與局時

- 個人混戰：最後存活者勝；同一 tick 全滅算平手。
- 組隊：一方全隊淘汰即分出勝負。
- **單局定勝負**：一局結束就回到房間結算畫面，由房主按「再來一局」重開，不做三局兩勝或累計積分。
- **單機與線上規則完全一致**：單機一樣是 3 分鐘、一樣有水位上升，練習就是在練真正上場的情境。
- 局時上限 **3 分鐘**（確定值，不做房主可調）。時間剩 **45 秒** 進入 **「水位上升」**：地圖最外圈開始逐圈變成硬塊（每 1.2 秒一格，順時針），逼玩家往中間打，避免龜縮拖時間。
- 時間完全用盡仍未分勝負 → 依序比較：存活人數 → 破箱數 → 平手。
- **結算畫面**：勝利角色的大頭貼大大地歡呼，底下一行數據（存活時間、破箱數），
  再加「再來一局」與「回大廳」兩顆大按鈕。**不做完整排名表**，讓下一局能很快開始。

---

## 2. 道具

初始能力：水球 1 顆、威力 1 格、速度 3.2 格／秒。

**正面道具**

| 道具 | 效果 | 上限 |
|---|---|---|
| 水球 ＋1 | 同時能放的水球數 +1 | 8 |
| 威力 ＋1 | 水柱長度 +1 | 8 |
| 溜冰鞋 | 移動速度 +0.35 格／秒 | 6.0 格／秒 |
| 手套（踢） | 撞到水球可把它踢出去，直到撞牆、撞人或撞到另一顆水球 | 持有／可關閉 |
| 針 | 可以直接戳破自己踩到的泡泡（救援加速） | 持有 |
| 護盾糖 | 3 秒無敵 | 即時 |

**負面道具**（**預設開啟**；幼幼班強制關閉，房主也可在房間設定關閉）

| 道具 | 效果 |
|---|---|
| 烏龜 | 8 秒內速度 −40% |
| 迷你水球 | 8 秒內威力固定為 1 |
| 亂步鞋 | 5 秒內方向左右相反 |

- 掉落用 **可注入種子的亂數（`rng.js`）**，讓重播、AI 測試、多人同步都能重現。
- 道具被水柱炸到會消失（避免堆在安全區）。
- **被淘汰時噴出一半道具**（無條件捨去，負面道具不噴）到周圍最近的空地上，其他人可以去撿。
  補刀有回報、落後的人也有翻盤的機會；噴出的道具一樣會被水柱炸掉。
- **「手套（踢水球）」排到 M4 才實作**：移動中的水球要進預測、對帳與插值，是整套同步裡最容易出鬼影的一環，
  先把基本對戰與線上同步做穩，確認手感沒問題再加。在那之前手套不會出現在道具池裡。

---

## 3. 移動與連線同步（最關鍵的技術決定）

### 3.1 伺服器權威 + 固定 tick

- 伺服器以 **30 Hz（33.3 ms）** 跑 `stepMatch()`，是唯一的真相來源。
- 客戶端只送 **輸入意圖**：`{ seq, dir: {x, y}, drop: bool, dtMs }`，不送座標。
- 伺服器每 tick 廣播快照：`{ tick, players[], bombs[], blasts[], items[], tileEvents[] }`。
  - 玩家：位置（float，單位＝格）、速度、狀態（正常／泡泡／無敵／淘汰）、水球數、威力。
  - 地圖變動用**事件**傳（箱子破了、道具生成／被撿走），不每 tick 傳整張地圖。

### 3.2 前端手感

- **本地預測**：自己的角色照本地輸入立刻動，保留尚未被確認的 input queue。
- **對帳（reconciliation）**：收到快照後，把自己的位置校正到伺服器值，再重播未確認的 input。
- **其他玩家插值**：維持約 100 ms 的緩衝，在兩個快照之間平滑內插，不做外插（避免瞬移回彈）。
- **貼牆微調（corner assist）**：往前撞牆但斜前方是通道時，自動往通道方向修正，這是原版走位手感的關鍵，必須做。
- 碰撞：角色以 **0.7 格寬的圓／AABB** 對格子做碰撞，允許擦邊過。

### 3.3 單機與線上共用同一套規則

- `public/js/rules.js` 是**純函式規則核心**：無 DOM、無 socket、無計時器。
  - `createMatch(config, seed)` → `state`
  - `stepMatch(state, inputsByPlayer, dtMs)` → `{ state, events[] }`
  - `legalMoves / placeBubble / explode / resolveTrapped / checkWinner`
- 單機模式：瀏覽器自己跑 `stepMatch`，AI 產生和真人**格式完全相同**的 input intent。
- 線上模式：伺服器跑 `stepMatch`（Node 直接 require 同一支檔案）。
- **不會出現兩套規則互相漂移。**

### 3.4 斷線與重連

- **被淘汰的玩家自動轉成觀戰**，可以看完這一局並繼續聊天，不會被踢回房間；
  想提早離開隨時可以按「回房間」。局結束後所有人一起回到房間結算。
- 斷線後該玩家**維持在場但停止移動**（不會自動送死），保留 **30 秒**。
- **30 秒內重連**：補一份 **full snapshot**（含整張地圖）再接續增量，直接回到自己的角色繼續打。
- **超過 30 秒**：**直接判定離場**（視同淘汰、釋出席位）。玩家之後重連會以**觀戰者**身分回到房間，下一局再入座。
- 不做 AI 接管，避免「電腦幫你打贏」的爭議與額外的權限判定。

---

## 4. AI（四段，差異必須看得出來）

共用基礎：每 tick 由規則核心算出 **危險圖（danger map）**——每一格「幾秒後會被炸到」，再用 BFS 找最近的安全格。

| 難度 | 行為 |
|---|---|
| **幼幼班** | 只撿道具與炸箱子，**幾乎不主動攻擊玩家**；看到玩家會退開；玩家被困成泡泡時**不會補刀**；自己逃跑路徑寬鬆（有 1 格容錯）。目標是讓 3～5 歲小朋友幾乎一定會贏 |
| **簡單** | 會炸箱子搶道具，反應延遲 **0.6 秒**，只看 1 步安全，常常自己把自己堵死 |
| **普通** | BFS 完整逃生路徑、會在走廊堵人、反應延遲 **0.3 秒**、會補刀泡泡 |
| **困難** | 預測玩家 1 秒內的可能位置、會算**連鎖引爆**、會封死走廊與轉角、反應延遲 **0.12 秒**、組隊時會主動去救隊友、會刻意搶關鍵道具 |

- **每個電腦對手都有名字與難度**，名牌顯示成「小花貓（困難）」這種格式，
  名字從可愛動物名單裡挑（小花貓、阿布狗、雪球兔、胖胖熊……），一局內不重複。
- **幼幼班只弱化 AI，不動規則**：泡泡照樣會被補刀、水位上升照常、自己的水球一樣會炸到自己。
  小朋友練熟之後往上調難度不需要重新學規則，遊戲行為始終一致。
- 組隊模式人數不足時可用電腦補位（採用你選的四段難度設定）。
- 驗證方式：`scripts/ai-check.js` 跑 100 場 AI 互打，統計勝率與平均存活時間，四段之間必須呈現單調差異。

---

## 5. 線上：大廳、房間、觀戰、邀請

沿用 `lib/rooms.js` 的既有模式。

- **大廳**：房間列表（房名、模式、人數／上限、地圖來源、是否可觀戰、是否進行中）、快速加入、建立房間。
- **房主可設定**：模式（個人／組隊）、人數上限（2–8，**預設 4**）、地圖來源（固定地圖挑一張／隨機種子）、局時、負面道具開關、AI 補位與難度。
- **快速加入**：大廳有一顆「快速加入」大按鈕，一鍵丟進「人最多且還沒開始」的房；
  真的沒有房就**自動幫你開一間**並等人加入，不會讓人卡在空蕩蕩的列表前面。
- **房間隱私**：一律公開房，全部列在大廳；要私下開打就用邀請連結。**不做密碼房**。
- **角色**：玩家席位／觀戰者，畫面上明確顯示自己是哪一種；席位滿時自動轉為觀戰並明白告知，**絕不默默把觀戰者當玩家**。
- **觀戰者權限**：可以在聊天室發言（訊息標示「觀戰」），Summary 面板顯示**全場所有人**的道具與狀態（像轉播畫面）。
  觀戰者**不排隊入座**；想加入要等該局結束回到房間再挑空位。
- **邀請連結**：token 帶 scope（指向特定房間），**不設到期時間**——只要房間還在連結就有效，
  約晚上再玩不必重發。房主隨時可**撤銷並重發**新連結，房間被回收或解散時連結自動失效。
  加入前一律由伺服器驗證，對「已撤銷／房間已結束／房間不存在」給清楚可懂的提示。
- **聊天室**：房間內文字聊天，觀戰者可聊天但不影響對局。
  **自由輸入＋快捷短語兩種都做**：輸入框上方一排一點就送的短語按鈕（好啊！／救我！／小心！／等我一下／GG／哈哈），
  小朋友不用打字也能溝通；對戰中點短語不會叫出鍵盤、不會擋住盤面。
- **房主權限**：可以把玩家**踢出房間**（被踢的人回大廳，短時間內不能再進同一房）；
  **房主離開時自動把房主交給房內第一位玩家**，房間不會卡死。不做「房主強制把人改成觀戰」。

---

## 6. 畫面與操作

### 6.1 版面（依你的偏好：資訊欄放左邊、刻意小、盤面最大）

- **寬版（桌機／平板橫向）**：左側窄欄固定放
  - 上：對戰 Summary（剩餘時間、存活名單與狀態、自己的水球／威力／速度、連線與 AI 狀態、水位上升倒數）
  - 下：房間聊天室
  - 右側其餘空間**全部給盤面**，盤面盡量放大。
- **窄版（手機／平板直向）**：Summary 收成頂部一條精簡狀態列＋可展開面板；聊天室維持左下角浮動入口（含未讀數）；盤面依然優先。
- **手機直向仍然完整可玩**（盤面等比縮放、搖桿與按鈕壓在下方安全區），但第一次進入對戰時會跳一則
  **「轉成橫向比較好玩喔」** 的輕提示，可以直接關掉、也可以勾「不再顯示」。不強迫旋轉、不擋畫面。

### 6.2 操作

- **觸控（預設）**：左下虛擬搖桿＋右下大顆放水球按鈕；搖桿與按鈕都在安全區內、半透明、不擋盤面主要區域，且離開手指後淡出。
- **觸控（替代）**：設定裡可切換為「全螢幕滑動」——畫面任一處按住拖曳即走（相對位移），點擊放水球。切換即時生效並記住。
- **鍵盤**：方向鍵／WASD 移動，空白鍵放水球，Esc 開設定。
- **無障礙**：可開啟「高對比水柱」與「減少動態」。

### 6.2.5 首頁與單機設定

**首頁**：一張可愛的大主視覺圖（動物們抱著水球），底下三顆圓潤立體大按鈕——
**「一個人玩」／「跟別人玩」／「怎麼玩」**，右上角是設定。小朋友一眼就知道要按哪裡。

**同一台裝置只支援一位玩家**（不做同機雙人分鍵盤），想一起玩就各自用一台裝置進同一個房間。

**單機設定**：可選 **1～7 個電腦對手**，**每個都能單獨挑難度**（要一個幼幼班陪玩、一個困難來練也行），
再選地圖來源（固定地圖挑一張／隨機）與角色。上次的設定會記住，下次直接按開始。

### 6.3 開局流程

**全員按「準備好」，房主才能開始**：

- 房間裡每個玩家都有一顆大大的「準備好！」按鈕，按下去頭像亮起打勾。
- 全部玩家都準備後，房主的「開始遊戲」才會亮起。
- 開始後仍有 **3 秒倒數**讓大家把手指放好，倒數期間不能移動也不能放球。
- 觀戰者不算在準備人數內。中途有人加入席位，準備狀態全部重置。

### 6.4 角色識別

八人混戰時「找不到自己」是最常見的挫折，所以三層一起做：

- **自己腳下有發光圓環**（只有自己看得到，隨心跳微微脈動）——最主要的定位方式。
- **頭頂小名牌顯示暱稱**，自己的名牌用高亮色；人多時名牌會自動縮小、重疊時錯位避讓。
- **組隊模式再加隊伍顏色**：隊友腳下同色、對手另一色，不用讀名字就分得出敵我。
- **色彩輔助：一律用形狀輔助，不只靠顏色**（預設就這樣做，不需要另外開設定）——
  自己的光環是實心圓、隊友是虛線圓、對手是鋸齒圓；正面道具用圓形底、負面道具用三角形底；
  隊伍標記一邊是星星、一邊是方塊。色弱的人不靠顏色也分得出來。

暱稱與角色**記在本機**，下次自動帶入；**同一房間允許多人選同一隻角色**，靠腳下光環與名牌區分。

### 6.5 教學

**靜態圖文說明頁**：一頁式 SVG 圖解，內容為「怎麼走、怎麼放水球、水柱怎麼延伸、被困住怎麼辦、道具圖鑑（正面／負面分開）、勝負與水位上升」。
第一次進入遊戲自動開啟一次，之後可從主畫面與設定彈窗隨時叫出來。不做互動式練習關。

### 6.6 設定（右上角按鈕 → Modal）

背景音樂開關／音量、音效開關／音量、震動、減少動態、操作方式（搖桿／滑動）、恢復預設。
**背景音樂與音效預設都是開的**（瀏覽器規定要先有一次點擊才會真的出聲，所以首次手勢後才解鎖播放）。
色彩輔助不放在設定裡——形狀區分是預設就有的設計，不需要開關。
支援首次手勢解鎖音訊、立即套用、重進仍保留、焦點鎖定、Esc／返回鍵關閉。

---

## 7. 視覺與資產

- 全部**手繪向量 SVG**，圓潤可愛、立體按鈕（沿用既有 `svgui.js` 風格）。
- **角色 8 隻——可愛動物園**（外觀不同、能力完全相同）：貓、狗、兔、熊、熊貓、企鵝、青蛙、小雞。
  每隻主色明顯拉開（避免同色系誤判隊友／對手），各自有走路、放球、泡泡、無敵閃爍、淘汰倒地五種姿勢。
- **地圖主題：六套以上，且做成可持續擴充**。每套是「硬塊／軟箱／地板」三件組，首波規劃：
  草原、海灘、糖果屋、太空站、森林、雪國，之後可以再往上加（夜市、水族館、火山……）。
  主題只換皮、不改規則。
  資產一律以純資料檔放在 `public/js/themes/*.js` 並在 `themes.js` 註冊，和水果連連看同一套做法，
  加一套主題＝加一個資料檔，不用動遊戲邏輯。
- **可辨識性優先**：水柱、道具、泡泡的顏色與形狀刻意拉開差距，避免誤判。
- **音效以泡泡／水聲為主軸**：放水球是「咕嘟」、水柱噴發是「嘩」、泡泡困住是「啵嗡」、掙脫與被救是「啵！」、
  撿道具是清脆水滴、水位上升是低沉水流聲。BGM 走輕快帶水感的可愛曲風。
- 全部使用可替換、授權清楚的資產；正式音訊如需你這邊提供，會在交付時列出。

---

## 8. 檔案結構（預定）

```
bubble-battle/
├─ server.js                  Express + Socket.IO、30Hz 房間迴圈
├─ lib/
│  ├─ rooms.js                房間、席位、觀戰、邀請 token、權限
│  └─ match-loop.js           伺服器端 tick 迴圈與快照廣播
├─ public/
│  ├─ index.html
│  ├─ css/
│  └─ js/
│     ├─ config.js            唯一 server URL 入口（GAME_SERVER_URL）
│     ├─ rules.js             ★ 共用純函式規則核心
│     ├─ rng.js               可注入種子亂數
│     ├─ maps.js              固定地圖資料 + 隨機地圖生成器
│     ├─ ai.js                四段 AI
│     ├─ net.js               預測／對帳／插值
│     ├─ online.js            socket 事件
│     ├─ render.js            SVG／Canvas 繪圖
│     ├─ svgui.js             立體按鈕與共用 UI
│     ├─ input.js             鍵盤＋虛擬搖桿＋滑動
│     ├─ audio.js  storage.js  app.js   （storage.js 兼管本機戰績）
│     └─ themes/*.js          地圖與角色美術資料
├─ tests/verify.js            規則核心單元測試
├─ scripts/
│  ├─ online-check.js  reconnect-check.js  invite-nick-check.js
│  ├─ sync-check.js           預測／對帳一致性
│  ├─ ai-check.js             四段 AI 勝率差異
│  ├─ board-check.js  settings-check.js  sidebar-check.js
│  ├─ browser-check.js        Playwright 版面與觸控
│  └─ inject-server-url.js
├─ .env.example  .gitignore  render.yaml  README.md  啟動遊戲.bat
└─ .github/workflows/         GitHub Pages 佈署（注入 GAME_SERVER_URL）
```

---

## 9. 里程碑（薄切優先，每階段都能玩）

| 階段 | 內容 | 驗收 |
|---|---|---|
| **M0** | 規則核心＋單機一局：走位、放球、爆炸、泡泡、淘汰、勝負、重新開始 | 一個人能從頭玩到結算 |
| **M1** | 道具、固定＋隨機地圖、四段 AI、音效、設定彈窗 | 四段 AI 勝率差異可觀察 |
| **M2** | 線上：大廳、房間、2–4 人個人混戰、30Hz 同步、預測／插值、重連 | 兩台裝置同房順暢對戰 |
| **M3** | 組隊 2v2／3v3、6–8 人、觀戰、邀請連結、聊天室、Summary 面板 | 角色權限與邀請流程正確 |
| **M4** | RWD（平板＞桌機＞手機、直橫向）、美術打磨、教學、完整測試、Render＋Pages 佈署準備、**把卡片與連結加進既有 `game-lobby`** | `npm run verify` 全綠、大廳可直接進入 |

---

## 10. 部署

- 伺服器：**Render 免費方案**（會休眠／冷啟動 → 房間狀態視為可清理的暫時狀態，不寫本機磁碟；提供 `/health`）。
- **冷啟動體驗**：伺服器休眠後第一個人進大廳要等約 30～60 秒。這段時間畫面顯示
  **「喚醒伺服器中」的泡泡動畫**＋等待秒數與「第一次進來會等一下下」的說明，
  背景持續輪詢 `/health`，成功就自動進入大廳；逾時給明確錯誤與「再試一次」。
- 前端：**GitHub Pages + GitHub Actions**，建置時由 repo 變數 `GAME_SERVER_URL` 注入到 `public/js/config.js`。
- 正式環境走 HTTPS／WSS，明確設定允許的前端 origin／CORS，**不回退 localhost、不硬編碼網址**。
- **版控**：一開始就在 `C:\Eric\AI Agent\bubble-battle` 建立 git 並推上**公開 GitHub repo**（比照 fruit-link 的做法），
  含 `.gitignore`、`.env.example`、lockfile；秘密與本機設定不進版控。
- 建立 Render 服務、設定網域或正式發布前仍會先跟你確認。

---

## 10.5 本機戰績

用 `localStorage` 存一份**只在這台裝置上**的簡單統計，不需要帳號、不上傳伺服器、不做排行榜：

- 總場數、勝場、勝率
- 最長存活時間
- 累計破箱數、累計撿到的道具數
- 最常用的角色

主畫面可以看，設定彈窗提供「清除紀錄」。

---

## 10.6 語言

**只做繁體中文**，和你其他小遊戲一致：介面文案、程式註解、README、commit 訊息全部繁中，
文案直接寫在畫面上，不抽語言檔、不做語言切換。

---

## 11. 開工前的最後準備

規劃階段的問題都已經確認完畢（見第 0 節）。實作前只剩兩件事需要你動手或決定：

1. **GitHub repo**：名稱確定為 **`bubble-battle`**（公開），本機資料夾也已經改名為
   `C:\Eric\AI Agent\bubble-battle`。建立與 push 需要你這邊的 GitHub 帳號授權，
   我不會自行建立或 push，會先把專案整理成可直接 `git remote add` 的狀態再請你點頭。
2. **正式美術與音訊**：角色、地圖主題、音效我會先做出可用的手繪 SVG 與可替換的暫用音效；
   若你之後要換成指定資產，資料檔的格式（`public/js/themes/*.js` 純資料）會保持可替換。

確認以上兩點後，就從 **M0（單機一局可玩的薄切）** 開始實作。
